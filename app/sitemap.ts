import type { MetadataRoute } from "next"; import { concerns,programs,resources,services,specialists } from "@/data/site"; export default function sitemap():MetadataRoute.Sitemap{const base="https://ulamseyal.in";const routes=["","/about","/contact","/book-consultation","/mental-health","/services","/specialists","/programs","/resources","/assessments",...services.map(x=>'/services/'+x.slug),...concerns.map(x=>'/mental-health/'+x[0]),...specialists.map(x=>'/specialists/'+x.slug),...resources.map(x=>'/resources/'+x[0]),...programs.map(x=>'/programs/'+x[0])];return routes.map(url=>({url:base+url,lastModified:new Date(),changeFrequency:url===""?"weekly":"monthly",priority:url===""?1:.7}))}
