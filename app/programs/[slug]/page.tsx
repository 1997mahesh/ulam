import { notFound } from "next/navigation"; import { DetailPage } from "@/components/inner"; import { programs } from "@/data/site"; export function generateStaticParams(){return programs.map(x=>({slug:x[0]}))} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=programs.find(p=>p[0]===slug);if(!x)notFound();return <DetailPage kind="Institutional program" title={x[1]} intro={x[2]}/>}
