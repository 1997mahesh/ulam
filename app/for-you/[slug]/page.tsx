import { DetailPage } from "@/components/inner"; const labels:Record<string,string>={children:"Children",teenagers:"Teenagers",students:"Students",adults:"Adults",couples:"Couples",parents:"Parents",professionals:"Working Professionals",families:"Families",organisations:"Organisations"}; export function generateStaticParams(){return Object.keys(labels).map(slug=>({slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=labels[slug]||slug.replaceAll("-"," ");return <DetailPage kind="Support for you" title={'Psychological support for '+title} intro="Care that respects your stage of life, relationships, responsibilities and individual circumstances."/>}
