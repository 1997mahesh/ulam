import Link from "next/link";import {ArrowRight,ArrowUpRight,Brain,Clock3,HandHeart,HeartHandshake,Ribbon,UsersRound} from "lucide-react";import {PageHero} from "@/components/inner";import {prisma} from "@/lib/prisma";export const dynamic="force-dynamic";
function serviceIcon(slug:string){if(slug.includes("couple")||slug.includes("family"))return UsersRound;if(slug.includes("oncology")||slug.includes("cancer"))return Ribbon;if(slug.includes("individual"))return Brain;if(slug.includes("child")||slug.includes("adolescent"))return HandHeart;return HeartHandshake}
export default async function Services(){const rows=await prisma.service.findMany({where:{isActive:true},orderBy:[{displayOrder:"asc"},{name:"asc"}]});return <><PageHero eyebrow="Services" title="Ways we can support you" text="Individual, relational and psychological care designed for real lives and different needs."/><section className="services-page-section"><div className="container"><div className="services-page-intro"><div><p className="eyebrow">Explore our care</p><h2>Find support that fits your needs.</h2></div><p>Every service begins with listening. Explore the options below to understand what may feel most relevant, or speak with our team if you are unsure where to begin.</p></div>{rows.length?<div className="services-page-grid">{rows.map((service,index)=>{const Icon=serviceIcon(service.slug);return <Link className="service-page-card" href={`/services/${service.slug}`} key={service.id}><div className="service-card-top"><span className="service-card-icon"><Icon/></span><span className="service-card-number">{String(index+1).padStart(2,"0")}</span></div><p className="service-duration"><Clock3/>{service.durationMinutes} minutes</p><h2>{service.name}</h2><p className="service-description">{service.shortDescription}</p><span className="service-explore">Explore service <ArrowUpRight/></span></Link>})}</div>:<p className="admin-empty">Services will be available soon.</p>}<div className="services-guidance"><div><p className="eyebrow">Not sure which service is right?</p><h2>Let&apos;s find a thoughtful next step together.</h2><p>Tell us a little about what you are looking for and our team can help guide you towards an appropriate service or professional.</p></div><Link href="/book-consultation">Book a Consultation <ArrowRight/></Link></div></div></section></>}
