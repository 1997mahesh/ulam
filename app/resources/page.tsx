import Link from "next/link";import {PageHero} from "@/components/inner";import {prisma} from "@/lib/prisma";export const dynamic="force-dynamic";export default async function Resources(){const rows=await prisma.resource.findMany({where:{status:"PUBLISHED"},include:{category:true},orderBy:{publishedAt:"desc"}});return <><PageHero eyebrow="Resources" title="Understand your mind better" text="Clear, thoughtful guides about therapy, emotional health and everyday wellbeing."/><section className="section"><div className="container grid gap-5 md:grid-cols-2">{rows.map(x=><Link className="card p-8" href={`/resources/${x.slug}`} key={x.id}><p className="eyebrow">{x.category?.name||"Guide"}</p><h2 className="mt-5 text-3xl">{x.title}</h2><p className="mt-4">{x.excerpt}</p></Link>)}</div></section></>}
