import {notFound} from "next/navigation";import {Breadcrumb} from "@/components/ui";import {prisma} from "@/lib/prisma";export const dynamic="force-dynamic";export default async function Resource({params}:{params:Promise<{slug:string}>}){const{slug}=await params,r=await prisma.resource.findFirst({where:{slug,status:"PUBLISHED"}});if(!r)notFound();return <article className="section"><div className="container max-w-4xl"><Breadcrumb current={r.title}/><p className="eyebrow">Resource</p><h1 className="display mt-4">{r.title}</h1><p className="lede mt-6">{r.excerpt}</p><div className="resource-body mt-10 whitespace-pre-wrap leading-8">{r.body}</div></div></article>}
