import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import {TestimonialForm} from "../../testimonial-form";export const dynamic="force-dynamic";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params,t=await prisma.testimonial.findUnique({where:{id}});if(!t)notFound();return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Testimonials / Edit</p><h1>Edit Testimonial</h1></div></div><TestimonialForm item={t}/></section>}
