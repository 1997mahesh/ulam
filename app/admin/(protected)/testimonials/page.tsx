import {prisma} from "@/lib/prisma";import {RecordActions} from "@/components/admin/record-actions";import {deleteTestimonial} from "../delete-actions";import {TestimonialForm} from "./testimonial-form";export const dynamic="force-dynamic";export default async function Testimonials(){const rows=await prisma.testimonial.findMany({orderBy:{displayOrder:"asc"}});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Testimonials</p><h1>Testimonials</h1></div></div><TestimonialForm/><div className="admin-table-wrap"><table><thead><tr><th>Label</th><th>Context</th><th>Rating</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.nameOrAnonymousLabel}</td><td>{x.designationOrContext||"—"}</td><td>{x.rating||"—"}</td><td>{x.isPublished?"Published":"Hidden"}</td><td><RecordActions viewHref={`/admin/testimonials/${x.id}`} editHref={`/admin/testimonials/${x.id}/edit`} id={x.id} name={x.nameOrAnonymousLabel} action={deleteTestimonial} deleteLabel="Delete Testimonial"/></td></tr>)}</tbody></table></div></section>}
