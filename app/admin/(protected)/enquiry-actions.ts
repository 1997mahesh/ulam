"use server";import {EnquiryStatus} from "@prisma/client";import {revalidatePath} from "next/cache";import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/auth";import {prisma} from "@/lib/prisma";
export async function updateEnquiry(f:FormData){await requireAdmin();const id=String(f.get("id")),status=String(f.get("status")) as EnquiryStatus;if(!Object.values(EnquiryStatus).includes(status))throw new Error("Invalid status");await prisma.contactEnquiry.update({where:{id},data:{status,adminNotes:String(f.get("adminNotes")||"")}});revalidatePath("/admin/enquiries");redirect(`/admin/enquiries/${id}?notice=saved`)}
