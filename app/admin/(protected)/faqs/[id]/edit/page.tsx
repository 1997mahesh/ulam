import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import {FaqForm} from "../../faq-form";export const dynamic="force-dynamic";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params,f=await prisma.fAQ.findUnique({where:{id}});if(!f)notFound();return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / FAQs / Edit</p><h1>Edit FAQ</h1></div></div><FaqForm faq={f}/></section>}
