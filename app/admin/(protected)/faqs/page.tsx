import {prisma} from "@/lib/prisma";import {RecordActions} from "@/components/admin/record-actions";import {deleteFaq} from "../delete-actions";import {FaqForm} from "./faq-form";export const dynamic="force-dynamic";export default async function FAQs(){const rows=await prisma.fAQ.findMany({orderBy:{displayOrder:"asc"}});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / FAQs</p><h1>FAQs</h1></div></div><FaqForm/><div className="admin-table-wrap"><table><thead><tr><th>Question</th><th>Category</th><th>Status</th><th>Order</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.question}</td><td>{x.category||"—"}</td><td>{x.isPublished?"Published":"Hidden"}</td><td>{x.displayOrder}</td><td><RecordActions viewHref={`/admin/faqs/${x.id}`} editHref={`/admin/faqs/${x.id}/edit`} id={x.id} name={x.question} action={deleteFaq} deleteLabel="Delete FAQ"/></td></tr>)}</tbody></table></div></section>}
