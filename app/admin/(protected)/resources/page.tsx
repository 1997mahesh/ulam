import Link from "next/link";import {prisma} from "@/lib/prisma";import {RecordActions} from "@/components/admin/record-actions";import {deleteResource} from "../delete-actions";export const dynamic="force-dynamic";export default async function Resources(){const rows=await prisma.resource.findMany({orderBy:{updatedAt:"desc"}});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Resources</p><h1>Resources</h1></div><Link className="admin-primary" href="/admin/resources/new">Add Resource</Link></div><div className="admin-table-wrap"><table><thead><tr><th>Title</th><th>Status</th><th>Published</th><th>Updated</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.title}</b><small>{x.slug}</small></td><td>{x.status}</td><td>{x.publishedAt?.toLocaleDateString("en-IN")||"—"}</td><td>{x.updatedAt.toLocaleDateString("en-IN")}</td><td><RecordActions viewHref={`/admin/resources/${x.id}`} editHref={`/admin/resources/${x.id}/edit`} id={x.id} name={x.title} action={deleteResource} deleteLabel="Delete Resource"/></td></tr>)}</tbody></table></div></section>}
