import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import {ResourceForm} from "../../resource-form";export const dynamic="force-dynamic";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params,r=await prisma.resource.findUnique({where:{id}});if(!r)notFound();return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Resources / Edit</p><h1>{r.title}</h1></div></div><ResourceForm resource={r}/></section>}
