import {prisma} from "@/lib/prisma";import {CounsellorForm} from "../counsellor-form";export const dynamic="force-dynamic";export default async function New(){const services=await prisma.service.findMany({where:{isActive:true},select:{id:true,name:true},orderBy:{name:"asc"}});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Counsellors / New</p><h1>Add Counsellor</h1></div></div><CounsellorForm services={services}/></section>}
