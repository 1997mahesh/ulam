import Image from "next/image";import Link from "next/link";import {UserRound} from "lucide-react";import {prisma} from "@/lib/prisma";import {RecordActions} from "@/components/admin/record-actions";import {deleteCounsellor} from "../delete-actions";export const dynamic="force-dynamic";
export default async function Counsellors(){const rows=await prisma.counsellor.findMany({orderBy:[{displayOrder:"asc"},{name:"asc"}],include:{_count:{select:{services:true,appointments:true}}}});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Counsellors</p><h1>Counsellors</h1></div><Link className="admin-primary" href="/admin/counsellors/new">Add Counsellor</Link></div><div className="admin-table-wrap"><table><thead><tr><th>Photo</th><th>Name</th><th>Designation</th><th>Experience</th><th>Services</th><th>Bookings</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.photo?<Image className="admin-avatar" src={x.photo} width={46} height={46} alt=""/>:<span className="admin-avatar admin-avatar-empty"><UserRound/></span>}</td><td><b>{x.name}</b><small>{x.slug}</small></td><td>{x.designation}</td><td>{x.experienceYears} years</td><td>{x._count.services}</td><td>{x._count.appointments}</td><td>{x.isActive?"Active":"Inactive"}</td><td><RecordActions viewHref={`/admin/counsellors/${x.id}`} editHref={`/admin/counsellors/${x.id}/edit`} id={x.id} name={x.name} action={deleteCounsellor} deleteLabel="Delete Counsellor"/></td></tr>)}</tbody></table></div></section>}
