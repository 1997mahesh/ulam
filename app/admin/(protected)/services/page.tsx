import Link from "next/link";import {prisma} from "@/lib/prisma";import {RecordActions} from "@/components/admin/record-actions";import {deleteService} from "../delete-actions";export const dynamic="force-dynamic";
export default async function Services(){const rows=await prisma.service.findMany({orderBy:[{displayOrder:"asc"},{name:"asc"}],include:{_count:{select:{Counsellors:true,appointments:true}}}});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Services</p><h1>Services</h1></div><Link className="admin-primary" href="/admin/services/new">Add Service</Link></div><div className="admin-table-wrap"><table><thead><tr><th>Name</th><th>Duration</th><th>Price</th><th>Counsellors</th><th>Bookings</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.name}</b><small>{x.slug}</small></td><td>{x.durationMinutes} min</td><td>{x.price?`₹${x.price}`:"—"}</td><td>{x._count.Counsellors}</td><td>{x._count.appointments}</td><td>{x.isActive?"Active":"Inactive"}</td><td><RecordActions viewHref={`/admin/services/${x.id}`} editHref={`/admin/services/${x.id}/edit`} id={x.id} name={x.name} action={deleteService} deleteLabel="Delete Service"/></td></tr>)}</tbody></table></div></section>}
