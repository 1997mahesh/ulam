import {prisma} from "@/lib/prisma";import {getSiteSettings} from "@/lib/site-settings";import {RecordActions} from "@/components/admin/record-actions";import {saveAvailability} from "../content-actions";import {deleteAvailability} from "../delete-actions";export const dynamic="force-dynamic";const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
export default async function Availability(){const[Counsellors,rows,settings]=await Promise.all([prisma.counsellor.findMany({where:{isActive:true},orderBy:{name:"asc"}}),prisma.availability.findMany({include:{counsellor:true},orderBy:[{counsellorId:"asc"},{dayOfWeek:"asc"}]}),getSiteSettings()]);return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Availability</p><h1>Availability</h1></div></div><form action={saveAvailability} className="admin-panel admin-inline-form"><select name="counsellorId" required><option value="">Select counsellor</option>{Counsellors.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><select name="dayOfWeek">{days.map((x,i)=><option value={i} key={x}>{x}</option>)}</select><input type="time" name="startTime" required/><input type="time" name="endTime" required/><input type="number" name="slotDurationMinutes" defaultValue={settings.defaultDuration} min="15"/><button className="admin-primary">Add hours</button></form><div className="admin-table-wrap"><table><thead><tr><th>Counsellor</th><th>Day</th><th>Hours</th><th>Slot duration</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.counsellor.name}</td><td>{days[x.dayOfWeek]}</td><td>{x.startTime}–{x.endTime}</td><td>{x.slotDurationMinutes} min</td><td>{x.isActive?"Active":"Inactive"}</td><td><RecordActions viewHref={`/admin/availability/${x.id}`} editHref={`/admin/availability/${x.id}/edit`} id={x.id} name={`${x.counsellor.name} ${days[x.dayOfWeek]}`} action={deleteAvailability} deleteLabel="Delete Availability"/></td></tr>)}</tbody></table></div></section>}
