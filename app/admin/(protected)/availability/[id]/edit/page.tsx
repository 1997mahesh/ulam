import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import {saveAvailability} from "../../../content-actions";export const dynamic="force-dynamic";const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params,[a,Counsellors]=await Promise.all([prisma.availability.findUnique({where:{id},include:{counsellor:true}}),prisma.counsellor.findMany({orderBy:{name:"asc"}})]);if(!a)notFound();return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Availability / Edit</p><h1>{a.counsellor.name}</h1></div></div><form action={saveAvailability} className="admin-panel admin-edit"><input type="hidden" name="id" value={a.id}/><label>Counsellor<select name="counsellorId" defaultValue={a.counsellorId}>{Counsellors.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Day<select name="dayOfWeek" defaultValue={a.dayOfWeek}>{days.map((x,i)=><option value={i} key={x}>{x}</option>)}</select></label><label>Start<input name="startTime" type="time" defaultValue={a.startTime}/></label><label>End<input name="endTime" type="time" defaultValue={a.endTime}/></label><label>Slot duration<input name="slotDurationMinutes" type="number" min="15" defaultValue={a.slotDurationMinutes}/></label><input type="hidden" name="isActive" value={a.isActive?"on":"off"}/><button className="admin-primary">Save availability</button></form></section>}
