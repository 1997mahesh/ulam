import Link from "next/link";import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";export const dynamic="force-dynamic";const days=["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];export default async function View({params}:{params:Promise<{id:string}>}){const{id}=await params,a=await prisma.availability.findUnique({where:{id},include:{counsellor:true}});if(!a)notFound();return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Availability / View</p><h1>{a.counsellor.name}</h1></div><Link className="admin-primary" href={`/admin/availability/${a.id}/edit`}>Edit Availability</Link></div><section className="admin-panel"><dl><dt>Day</dt><dd>{days[a.dayOfWeek]}</dd><dt>Start</dt><dd>{a.startTime}</dd><dt>End</dt><dd>{a.endTime}</dd><dt>Slot duration</dt><dd>{a.slotDurationMinutes} minutes</dd><dt>Status</dt><dd>{a.isActive?"Active":"Inactive"}</dd></dl></section></section>}
