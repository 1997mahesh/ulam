"use client";import {useActionState} from "react";import {updateAccount} from "../cms-actions";export function AccountForm({name,email}:{name:string;email:string}){const[state,action,pending]=useActionState(updateAccount,null);return <form action={action} className="admin-panel admin-edit"><label>Name<input name="name" defaultValue={name} required/></label><label>Email<input name="email" type="email" defaultValue={email} required/></label><label>Current password<input name="currentPassword" type="password" required/></label><label>New password (optional)<input name="newPassword" type="password" minLength={12}/></label><label>Confirm new password<input name="confirmPassword" type="password" minLength={12}/></label>{state?.error&&<p role="alert">{state.error}</p>}{state?.success&&<p role="status">{state.success}</p>}<button className="admin-primary" disabled={pending}>{pending?"Saving…":"Update account"}</button></form>}
