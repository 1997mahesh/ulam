import {notFound} from "next/navigation";import {EnquiryStatus} from "@prisma/client";import {prisma} from "@/lib/prisma";import {updateEnquiry} from "../../enquiry-actions";export const dynamic="force-dynamic";export default async function Enquiry({params}:{params:Promise<{id:string}>}){const{id}=await params,e=await prisma.contactEnquiry.findUnique({where:{id}});if(!e)notFound();return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Enquiries / View</p><h1>{e.subject}</h1></div></div><section className="admin-panel"><dl><dt>Name</dt><dd>{e.name}</dd><dt>Email</dt><dd>{e.email}</dd><dt>Phone</dt><dd>{e.phone||"—"}</dd><dt>Message</dt><dd>{e.message}</dd></dl></section><form action={updateEnquiry} className="admin-panel admin-edit"><input type="hidden" name="id" value={e.id}/><label>Status<select name="status" defaultValue={e.status}>{Object.values(EnquiryStatus).map(x=><option key={x}>{x}</option>)}</select></label><label>Private notes<textarea name="adminNotes" rows={5} defaultValue={e.adminNotes||""}/></label><button className="admin-primary">Save enquiry</button></form></section>}
