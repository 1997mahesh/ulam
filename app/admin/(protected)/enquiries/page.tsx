import {prisma} from "@/lib/prisma";import {RecordActions} from "@/components/admin/record-actions";import {deleteEnquiry} from "../delete-actions";export const dynamic="force-dynamic";export default async function Enquiries(){const rows=await prisma.contactEnquiry.findMany({orderBy:{createdAt:"desc"},take:50});return <section className="admin-page"><div className="admin-page-title"><div><p>Admin / Enquiries</p><h1>Enquiries</h1></div></div><div className="admin-table-wrap"><table><thead><tr><th>Name</th><th>Contact</th><th>Subject</th><th>Status</th><th>Received</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.email}<small>{x.phone}</small></td><td>{x.subject}</td><td>{x.status}</td><td>{x.createdAt.toLocaleDateString("en-IN")}</td><td><RecordActions viewHref={`/admin/enquiries/${x.id}`} id={x.id} name={`${x.name}: ${x.subject}`} action={deleteEnquiry} deleteLabel="Delete Enquiry"/></td></tr>)}</tbody></table></div></section>}
