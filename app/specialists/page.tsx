import { SpecialistCard } from "@/components/cards"; import { PageHero } from "@/components/inner"; import { specialists } from "@/data/site"; export const metadata={title:"Our Specialists"}; export default function Page(){return <><PageHero eyebrow="Our team" title="Professionals who listen before they guide" text="Browse fictional demonstration profiles. Final credentials and registrations should be verified before launch."/><section className="section"><div className="container grid gap-5 md:grid-cols-2 lg:grid-cols-3">{specialists.map(p=><SpecialistCard key={p.slug} person={p}/>)}</div></section></>}
