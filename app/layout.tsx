import type {Metadata} from "next";import "./globals.css";import {SiteChrome} from "@/components/site-chrome";import {getSiteSettings} from "@/lib/site-settings";
export async function generateMetadata():Promise<Metadata>{const settings=await getSiteSettings();return{metadataBase:new URL("https://ulamseyal.in"),title:{default:settings.metaTitle,template:`%s | ${settings.siteName}`},description:settings.metaDescription,openGraph:{title:`${settings.siteName} — ${settings.tagline}`,description:settings.metaDescription,type:"website"}}}
export default async function RootLayout({children}:{children:React.ReactNode}){const settings=await getSiteSettings(),schema={"@context":"https://schema.org","@type":"ProfessionalService",name:settings.siteName,description:settings.metaDescription,url:"https://ulamseyal.in",telephone:settings.phone,email:settings.email,address:settings.address,sameAs:[settings.socialInstagram,settings.socialLinkedIn,settings.socialYouTube,settings.socialFacebook,settings.socialX].filter(Boolean)};return <html lang="en"><body><SiteChrome settings={settings}>{children}</SiteChrome><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></body></html>}
