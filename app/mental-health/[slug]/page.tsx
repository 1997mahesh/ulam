import { notFound } from "next/navigation"; import { DetailPage } from "@/components/inner"; import { concerns } from "@/data/site"; export function generateStaticParams(){return concerns.map(x=>({slug:x[0]}))} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=concerns.find(x=>x[0]===slug);if(!item)notFound();return <DetailPage kind="Mental health" title={item[1]} intro={item[2]}/>}
