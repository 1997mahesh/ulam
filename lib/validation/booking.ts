import {z} from "zod";
export const bookingSchema=z.object({patientName:z.string().trim().min(2).max(100),patientEmail:z.string().email().max(200),patientPhone:z.string().trim().min(7).max(20),patientAge:z.coerce.number().int().min(1).max(120).optional(),serviceId:z.string().cuid().optional().or(z.literal("")),counsellorId:z.string().cuid().optional().or(z.literal("")),appointmentDate:z.coerce.date().refine(x=>x>=new Date(new Date().setHours(0,0,0,0)),"Date must not be in the past"),startTime:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/),consultationMode:z.enum(["ONLINE","IN_PERSON"]),patientMessage:z.string().max(2000).optional()});
