"use client"; import { useState } from "react"; import { Plus } from "lucide-react";
export function Accordion({items}:{items:readonly (readonly [string,string])[]}){const [open,setOpen]=useState(0);return <div className="border-t border-[#cad6d1]">{items.map(([q,a],i)=><div key={q} className="border-b border-[#cad6d1]"><button aria-expanded={open===i} onClick={()=>setOpen(open===i?-1:i)} className="flex w-full items-center justify-between gap-5 py-6 text-left font-semibold"><span>{q}</span><Plus size={18} className={`shrink-0 transition ${open===i?"rotate-45":""}`}/></button>{open===i&&<p className="max-w-3xl pb-6 text-sm leading-7 text-[#60726d]">{a}</p>}</div>)}</div>}
