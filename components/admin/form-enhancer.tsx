"use client";
import {useEffect} from "react";
function cancelDestination(pathname:string){if(pathname.includes("/bookings/"))return"/admin/bookings";if(pathname.includes("/enquiries/"))return"/admin/enquiries";if(pathname.endsWith("/edit")){const parts=pathname.split("/");return parts.slice(0,-2).join("/")||"/admin"}if(pathname.endsWith("/new"))return pathname.slice(0,-4);return"/admin"}
export function AdminFormEnhancer(){useEffect(()=>{const enhance=()=>document.querySelectorAll<HTMLFormElement>("form.admin-edit").forEach(form=>{if(form.querySelector(".admin-form-actions"))return;const original=form.querySelector<HTMLButtonElement>(":scope > button.admin-primary");if(!original)return;original.hidden=true;const row=document.createElement("div"),submit=document.createElement("button"),cancel=document.createElement("a");row.className="admin-form-actions";submit.type="submit";submit.className="admin-primary";submit.textContent=original.textContent||"Save";cancel.className="admin-cancel";cancel.textContent="Cancel";cancel.href=`${cancelDestination(window.location.pathname)}?notice=cancelled`;row.append(cancel,submit);form.append(row);form.addEventListener("submit",()=>{sessionStorage.setItem("adminSavePending","true");window.setTimeout(()=>{submit.disabled=true;submit.textContent="Saving…"},0)},{once:true})});enhance();const observer=new MutationObserver(enhance);observer.observe(document.body,{childList:true,subtree:true});return()=>observer.disconnect()},[]);return null}
