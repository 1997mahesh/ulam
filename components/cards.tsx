import Image from "next/image"; import Link from "next/link"; import { ArrowUpRight } from "lucide-react"; import type { ElementType } from "react";
export function ServiceCard({slug,title,text,icon:Icon}:{slug:string;title:string;text:string;icon:ElementType}){return <Link className="card group flex min-h-64 flex-col p-7" href={`/services/${slug}`}><span className="mb-auto flex h-11 w-11 items-center justify-center rounded-full bg-[#e6efe9] text-[#2f6157]"><Icon size={20}/></span><h3 className="mt-8 text-2xl">{title}</h3><p className="mt-3 text-sm leading-6 text-[#61736e]">{text}</p><span className="mt-5 flex items-center gap-2 text-xs font-bold">Explore care <ArrowUpRight size={14}/></span></Link>}
export function SpecialistCard({person}:{person:{slug:string;name:string;role:string;qualification:string;experience:string;languages:string;image:string}}){return <article className="card overflow-hidden"><Link href={`/specialists/${person.slug}`} className="image-cover block aspect-[4/4.3]"><Image src={person.image} fill sizes="(max-width: 768px) 100vw, 33vw" alt={`${person.name}, ${person.role}`} className="transition duration-500 hover:scale-[1.03]"/></Link><div className="p-6"><p className="eyebrow">{person.role}</p><h3 className="mt-2 text-2xl">{person.name}</h3><p className="mt-2 text-xs text-[#647671]">{person.qualification} · {person.experience}</p><div className="mt-5 flex items-center justify-between border-t border-[#e1e7e3] pt-4"><Link className="text-xs font-bold" href={`/specialists/${person.slug}`}>View profile</Link><Link className="text-xs font-bold text-[#a76535]" href={`/book-consultation?specialist=${person.slug}`}>Book consultation</Link></div></div></article>}
